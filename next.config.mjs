/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'dbsidzbcdajywjvrrynw.supabase.co', 'insur-hotel.s3.ap-south-1.amazonaws.com'],
    },
};

export default nextConfig;
